dist: bionic
language: python
python:
- '3.6'
- '3.7'
services:
    - docker
before_install:
  - pip install -r requirements.txt
  - cd apache_web_server
  - docker-compose up --build -d
script:
  - pytest tests/
  - flake8 . --count --show-source --statistics
after_script:
  - docker-compose down
